An apparent bug in the eclipse IDE requires that the .s file in this folder be renamed to .S (capitalized) in order to work correctly.

The .s file and the .ld file are copied from files generated by STM32CUBEMX.